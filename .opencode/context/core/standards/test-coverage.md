# Test Coverage Standards

- Prefer deterministic tests (no network, no real time dependencies).
- Cover success + failure cases for critical behaviors.
- Keep test naming and structure consistent with the repo.
